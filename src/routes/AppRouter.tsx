import { BrowserRouter } from 'react-router-dom';
import Router from './Router';

const AppRouter = () => {
  return (
    <BrowserRouter>
      <Router />
    </BrowserRouter>
  );
};

export default AppRouter;
